---
name: 图片瓦片生成库
overview: 实现一个由 Rust 驱动的高性能图片瓦片生成库，采用类似 esbuild 的二进制分发策略，通过 JSON stdio 与 Node.js 通信，支持 ZXY 瓦片结构生成、多种输出格式，并提供 CLI 和 SDK。
todos:
  - id: rust-core
    content: 实现 Rust 核心：图像处理、瓦片生成、JSON stdio 通信
    status: completed
  - id: rust-test
    content: 本地编译测试 Rust 二进制功能
    status: completed
    dependencies:
      - rust-core
  - id: npm-sdk
    content: 实现 npm SDK：API 设计、二进制定位、任务池
    status: completed
    dependencies:
      - rust-test
  - id: npm-cli
    content: 实现 CLI 命令行工具
    status: completed
    dependencies:
      - npm-sdk
  - id: cross-compile
    content: 配置交叉编译和 CI 构建多平台二进制
    status: completed
    dependencies:
      - rust-test
  - id: publish
    content: 发布主包和平台特定包到 npm
    status: completed
    dependencies:
      - npm-cli
      - cross-compile
---

# 图片瓦片生成库实施计划

## 架构概览

```mermaid
flowchart TB
    subgraph npm_packages [npm 包结构]
        MainPkg["@yafh/image-map<br/>主包 SDK + CLI"]
        DarwinArm["@yafh/image-map-darwin-arm64"]
        DarwinX64["@yafh/image-map-darwin-x64"]
        LinuxX64["@yafh/image-map-linux-x64"]
        WinX64["@yafh/image-map-win32-x64"]
    end
    
    subgraph rust_core [Rust 核心]
        Binary["image-map 二进制"]
        ImageProc["图像处理模块"]
        TileGen["瓦片生成模块"]
        IOModule["JSON stdio 通信"]
    end
    
    MainPkg -->|optionalDependencies| DarwinArm
    MainPkg -->|optionalDependencies| DarwinX64
    MainPkg -->|optionalDependencies| LinuxX64
    MainPkg -->|optionalDependencies| WinX64
    
    MainPkg -->|spawn| Binary
    Binary --> ImageProc
    Binary --> TileGen
    Binary --> IOModule
```



## 第一阶段：Rust 核心实现

### 1.1 项目结构调整

重命名并重组 `src-native/` 目录：

```javascript
src-native/
├── Cargo.toml
├── src/
│   ├── main.rs          # CLI 入口，处理 stdio JSON 通信
│   ├── lib.rs           # 库入口
│   ├── tile/
│   │   ├── mod.rs       # 瓦片生成模块
│   │   ├── generator.rs # ZXY 瓦片生成器
│   │   └── config.rs    # 配置结构体
│   ├── image/
│   │   ├── mod.rs       # 图像处理模块
│   │   ├── resize.rs    # 缩放与锐化
│   │   └── encoder.rs   # 编码器（PNG/JPG/WebP）
│   └── protocol/
│       ├── mod.rs       # 通信协议
│       └── message.rs   # JSON 消息定义
```



### 1.2 核心依赖

```toml
[dependencies]
image = "0.25"           # 基础图像处理
fast_image_resize = "5"  # SIMD 加速缩放
rayon = "1.10"           # 并行处理
serde = { version = "1", features = ["derive"] }
serde_json = "1"         # JSON 序列化
clap = { version = "4", features = ["derive"] }  # CLI 参数解析
```



### 1.3 ZXY 瓦片生成算法

```mermaid
flowchart TD
    Input["输入图片"] --> Level0["Level 0: 原图切割"]
    Level0 --> Tiles0["瓦片 z=0"]
    
    Input --> Resize1["缩小 50% + 锐化"]
    Resize1 --> Level1["Level 1: 切割"]
    Level1 --> Tiles1["瓦片 z=1"]
    
    Resize1 --> ResizeN["继续缩小..."]
    ResizeN --> LevelN["Level N: 切割"]
    LevelN --> TilesN["瓦片 z=N"]
    
    subgraph output [输出结构]
        direction LR
        Z["z/"] --> X["x/"]
        X --> Y["y.ext"]
    end
```

**切割中心配置**：支持 `TopLeft`（左上角，Web 地图标准）和 `Center`（中心对齐）。

### 1.4 通信协议设计

采用行分隔的 JSON 消息（NDJSON）：

```json
// 请求
{"type": "generate", "id": "uuid", "input": "/path/to/image.jpg", "output": "/output/dir", "options": {"tileSize": 256, "formats": ["webp"], "origin": "topLeft", "minZoom": 0, "maxZoom": 5}}

// 响应
{"type": "progress", "id": "uuid", "current": 50, "total": 100, "message": "Generating z=3..."}
{"type": "complete", "id": "uuid", "result": {"tilesGenerated": 341, "outputDir": "/output/dir"}}
{"type": "error", "id": "uuid", "error": "File not found"}
```

---

## 第二阶段：交叉编译与分发

### 2.1 目标平台

| 平台 | target triple | npm 包名 ||------|---------------|----------|| macOS ARM64 | aarch64-apple-darwin | @yafh/image-map-darwin-arm64 || macOS x64 | x86_64-apple-darwin | @yafh/image-map-darwin-x64 || Linux x64 | x86_64-unknown-linux-gnu | @yafh/image-map-linux-x64 || Windows x64 | x86_64-pc-windows-msvc | @yafh/image-map-win32-x64 |

### 2.2 交叉编译配置

使用 [cross](https://github.com/cross-rs/cross) 或 cargo-zigbuild 进行交叉编译：

```bash
# 安装 cross
cargo install cross

# 编译各平台
cross build --release --target aarch64-apple-darwin
cross build --release --target x86_64-unknown-linux-gnu
# ...
```



### 2.3 npm 包结构（类 esbuild 方案）

**主包** `@yafh/image-map`：

```json
{
  "optionalDependencies": {
    "@yafh/image-map-darwin-arm64": "1.0.0",
    "@yafh/image-map-darwin-x64": "1.0.0",
    "@yafh/image-map-linux-x64": "1.0.0",
    "@yafh/image-map-win32-x64": "1.0.0"
  }
}
```

**平台包目录结构**：

```javascript
packages/
├── image-map/                 # 主包
│   ├── package.json
│   ├── src/
│   │   ├── index.ts           # SDK 入口
│   │   ├── cli.ts             # CLI 入口
│   │   ├── binary.ts          # 二进制定位逻辑
│   │   ├── pool.ts            # 任务池管理器
│   │   └── protocol.ts        # 消息协议类型
│   └── bin/
│       └── image-map          # CLI shebang 脚本
├── image-map-darwin-arm64/    # 平台包
│   ├── package.json
│   └── bin/
│       └── image-map          # 预编译二进制
└── ...
```

---

## 第三阶段：npm SDK 实现

### 3.1 核心 API 设计

```typescript
// SDK 使用示例
import { ImageMap, createPool } from '@yafh/image-map'

// 单次调用
const result = await ImageMap.generate({
  input: './large-image.jpg',
  output: './tiles',
  tileSize: 256,
  formats: ['webp', 'jpg'],
  origin: 'topLeft',
  minZoom: 0,
  maxZoom: 8,
  onProgress: (current, total) => console.log(`${current}/${total}`)
})

// 池化任务管理
const pool = createPool({ concurrency: 4 })
pool.add({ input: 'image1.jpg', output: 'tiles1' })
pool.add({ input: 'image2.jpg', output: 'tiles2' })
await pool.run()
```



### 3.2 任务池实现

```mermaid
flowchart LR
    Queue["任务队列"] --> Pool["任务池<br/>concurrency=N"]
    Pool --> Worker1["Worker 1<br/>spawn binary"]
    Pool --> Worker2["Worker 2<br/>spawn binary"]
    Pool --> WorkerN["Worker N<br/>spawn binary"]
    Worker1 --> Result["结果收集"]
    Worker2 --> Result
    WorkerN --> Result









```